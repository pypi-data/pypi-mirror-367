[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tpcti"
version = "0.0.2"
description = "package"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  { name="khah", email="trchikhanhfrv@gmail.com" }
]
dependencies = []

[tool.setuptools]
package-dir = {"" = "."}
packages = ["tpcti"]
