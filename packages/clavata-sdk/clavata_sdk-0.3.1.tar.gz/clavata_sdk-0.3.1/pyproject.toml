[build-system]
requires = [ "poetry-core",]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "clavata-sdk"
version = "0.3.1"
description = "Clavata SDK for Python"
authors = [ "Brett Levenson <brett@clavata.ai>",]
packages = [
    { include = "clavata_sdk", from = "." },
    { include = "README.md", from = "." },
]

[tool.poetry.dependencies]
python = "^3.12"
grpcio = "^1.62.0"