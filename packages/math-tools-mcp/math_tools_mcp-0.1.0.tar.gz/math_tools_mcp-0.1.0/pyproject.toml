[project]
name = "math-tools-mcp"
version = "0.1.0"
description = "Add your description here"

authors = [{name = "Akashns007", email="akashns@thoughtclan.com"}]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.12.3",
    "numpy>=2.3.2",
    "sympy>=1.14.0",
]

[project.scripts]
math-tools-mcp = "sample_mcp_tests:main" 

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"