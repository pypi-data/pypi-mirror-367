# -*- coding: utf-8 -*-
from enum import Enum


class Tags(Enum):
    DOCUMENTS = "documents"
    DYNAMIC = "dynamic"
    FILES = "files"
    LANGCHAIN = "langchain"
    SPLITTERS = "splitters"
    TEXT = "text"
