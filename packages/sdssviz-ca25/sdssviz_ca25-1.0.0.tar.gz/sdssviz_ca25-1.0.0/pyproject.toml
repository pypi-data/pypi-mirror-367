[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sdssviz_ca25"
version = "1.0.0"

dependencies = [
 "numpy",
 "matplotlib",
 "astroquery",
 "mplcursors",
]
