[tool.poetry]
name = "pcservices-package"
version = "0.1.0"
description = "A package for PC Services data processing in Python and Pyspark"
authors = ["Gang Huang <gang.huang@loblaw.ca>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
pandas = "^2.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
