[project]
name = "leo-first-mcp-server"
version = "0.1.4"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Lee", email = "130852499+BaiCai625@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.12.3",
]

[project.scripts]
leo-first-mcp-server = "leo_first_mcp_server:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
