[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "GALINDA"
version = "1.0.1"

dependencies = [
 "numpy",
 "matplotlib",
 "ffmpeg",
 "h5py"
]
