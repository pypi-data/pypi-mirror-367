[project]
name = "sidetool"
version = "0.2.0"
description = "Python Automation tool for building PySide6 UI and PyInstaller EXE."
authors = [
  { name="Alan Lilly", email="panofish@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
