<!doctype html>
<html lang="${request.locale_name}">
    <head>
        <i tal:omit-tag="">${structure:tales:metas}</i>
        <i tal:omit-tag="">${tales:resources}</i>
    </head>
    <body>
        <div id="content">
            <div class="modal-dialog ${view.modal_class | ''}">
                <div class="modal-content ${view.modal_content_class | ''} p-1">
                    <i tal:omit-tag="">${structure:provider:pyams.header}</i>
                    <i tal:omit-tag="">${structure:provider:pagelet}</i>
                    <i tal:omit-tag="">${structure:provider:pyams.footer}</i>
                </div>
            </div>
        </div>
    </body>
</html>