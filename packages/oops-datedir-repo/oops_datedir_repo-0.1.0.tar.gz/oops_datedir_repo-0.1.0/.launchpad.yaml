pipeline:
  - lint
  - py38

jobs:
  lint:
    architectures: amd64
    packages:
      - tox
      - git
    run: tox -e lint
    series: focal
  py38:
    architectures: amd64
    packages:
      - tox
    run: tox -e py38
    series: focal
