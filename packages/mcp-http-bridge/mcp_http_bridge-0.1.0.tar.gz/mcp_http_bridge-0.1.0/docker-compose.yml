version: '3.8'

services:
  mcp-http-bridge:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount custom config to /app/config.json
      - ./config-example.json:/app/config.json:ro
    restart: unless-stopped
