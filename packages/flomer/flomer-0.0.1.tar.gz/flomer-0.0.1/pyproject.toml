[project]
name = "flomer"
version = "0.0.1"
description = "Developer-first tool for traceable record lifecycle and error logging"
authors = [{ name="Sergey Vystavkin", email="info@flomer.ai" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
