[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "footystats"
version = "0.2.2"
description = "grab soccer data and build databases"
authors = [{name = "MORENO", email = "m_moreno@hotmail.fr"}]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["."]
