[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "b10-tcache"
version = "1.0.2"
description = "PyTorch compilation cache for Baseten"
authors = ["coolbeans"]
packages = [{include = "b10_tcache", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
