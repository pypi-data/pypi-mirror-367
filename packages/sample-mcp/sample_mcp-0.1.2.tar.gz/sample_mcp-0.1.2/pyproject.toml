[project]
name = "sample-mcp"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vilohit", email = "vilohit.tapashetti@thoughtclan.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.12.3",
    "numpy>=2.3.2",
    "sympy>=1.14.0",
]

[project.scripts]
sample-mcp = "sample_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
