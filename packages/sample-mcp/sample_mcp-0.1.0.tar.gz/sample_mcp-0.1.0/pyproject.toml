[project]
name = "sample_mcp"
version = "0.1.0"
description = "Tool checking for mcp"
authors = [
    { name = "Nikitha63", email = "nikithauma6@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.12.3",
    "numpy>=2.3.2",
    "sympy>=1.14.0",
]
[project.scripts]
sample_mcp= "sample_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
