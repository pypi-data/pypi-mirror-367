[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simplecalctest"
version = "0.1.0"
description = "A simple calculator library for addition and subtraction"
authors = [
    { name="Mvenyi Donald", email="mvenyidonny1987@gmail.com" }
]
license = {file = "LICENSE"}
readme = "README.md"
requires-python = ">=3.7"
dependencies = []