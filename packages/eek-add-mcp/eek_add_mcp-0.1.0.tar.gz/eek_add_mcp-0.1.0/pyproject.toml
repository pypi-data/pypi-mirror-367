[project]
name = "eek-add-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "eeknight666", email = "eeknight666@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.12.3",
]

[project.scripts]
eek-add-mcp = "eek_add_mcp:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
