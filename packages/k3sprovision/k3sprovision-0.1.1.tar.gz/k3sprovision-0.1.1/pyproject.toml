[project]
name = "k3sprovision"
version = "0.1.1"
description = "A package for provisioning k3s clusters."
authors = [
    { name = "Omniopenverse", email = "omniopenverse@gmail.com" }
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"

[tool.setuptools]
packages = ["k3sprovision"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
