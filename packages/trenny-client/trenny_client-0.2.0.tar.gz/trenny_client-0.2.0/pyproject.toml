[project]
name = "trenny-client"
version = "0.2.0"
description = "TrennyClient for logging and verifying API keys"
authors = [{ name="Christian", email="chris@embotic.xyz" }]
dependencies = [
    "requests",
    "colorama"
]
readme = "README.md"
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/Chris-tian123/trenny-client"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"