"""This module is used to resolve the original suite2p version, the version of sun lab modifications and the version
of python on which the code runs. This information is added to each 'ops.npy' file generated by both single-day and
multi-day pipelines.
"""

import sys

from importlib_metadata import metadata as _metadata

version = "0.14.0"  # Statically set to the suite2p version against which this revision is built
# Version of this sl-modified package
sl_version = _metadata("sl-suite2p")["version"]  # type: ignore
python_version = f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}"  # Python version
