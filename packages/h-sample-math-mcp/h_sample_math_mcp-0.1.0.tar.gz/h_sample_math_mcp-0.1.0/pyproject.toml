[project]
name = "h-sample-math-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "harshitha", email = "harshithav79@yahoo.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.12.3",
    "sympy>=1.14.0",
]

[project.scripts]
h-sample-math-mcp = "h_sample_math_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
