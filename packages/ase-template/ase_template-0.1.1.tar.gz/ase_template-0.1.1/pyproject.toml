[project]
name = "ase-template"
version = "0.1.1"
description = "Agentic software engineering (ASE) template"
readme = "README.md"
authors = [{ name = "Cody", email = "cody@dkdc.dev" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ase-template = "ase_template:main"

[dependency-groups]
dev = ["pre-commit>=4.2.0", "ruff>=0.12.7"]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
