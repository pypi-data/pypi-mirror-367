[project]
name = "devenv-cli"
version = "0.1.2"
description = ""
authors = [
    {name = "Shuchir Jain",email = "pip@shuchir.dev"}
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "questionary (>=2.1.0,<3.0.0)",
    "docker (>=7.1.0,<8.0.0)",
    "typer (>=0.16.0,<0.17.0)",
    "yaspin (>=3.1.0,<4.0.0)"
]
packages = [{include = "devenv", from = "src"}]

[tool.poetry.scripts]
devenv = "devenv_cli.main:app"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
