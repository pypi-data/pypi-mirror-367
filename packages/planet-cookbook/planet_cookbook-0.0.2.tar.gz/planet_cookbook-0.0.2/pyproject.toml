[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "planet_cookbook"
version = "0.0.2"

dependencies = [
  "numpy",
  "astropy>=4",
  "numpy",
  "scipy",
  "pandas",
  "seaborn",
  "matplotlib",
  "rebound"
]