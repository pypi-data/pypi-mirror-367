[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "accessly"
version = "0.0.2"
dependencies = [
  "matplotlib",
  "numpy",
  "pyfonts",
  "pytest"
]
