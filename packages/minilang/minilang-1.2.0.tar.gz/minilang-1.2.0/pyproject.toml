[project]
name = "minilang"  # must be unique on PyPI
version = "1.2.0"
description = "A simple way to create a mini programing language"
authors = [
  { name="Bob Robertson", email="Coderdude123@proton.me" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = []

[project.urls]
Homepage = "https://github.com/Jeff-web-sketch/minilang_lib_for_python/tree/main/minilang"


[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
