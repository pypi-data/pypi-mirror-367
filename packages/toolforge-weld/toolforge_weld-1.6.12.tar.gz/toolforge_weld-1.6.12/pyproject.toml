[build-system]
# exists for tox
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "black"
skip_gitignore = true

[tool.black]
target_version = ["py37"]

[tool.mypy]
exclude = [
    "^debian/",
    "^.tox",
    "^venv",
]
