[project]
name = "naosconnector"
version = "0.1.5"
description = "helper class to connect to a Naos platform"
authors = [
    {name = "Nicolas Parment",email = "nparment@forsk.com"}
]
license = {text = "Commercial"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests (>=2.32.3,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
