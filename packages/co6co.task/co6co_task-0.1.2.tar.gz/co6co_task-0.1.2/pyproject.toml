[build-system]
requires = ["setuptools", "wheel", "co6co"]
build-backend = "setuptools.build_meta"

# 这里是模板，使用请改为[project]
[project_template]
name = "co6co.task"
version = "0.0.27"
dependencies = [
    "co6co",
    # 其他依赖项
]
