apiVersion: apps/v1
kind: Deployment
metadata:
  name: a2a-registry-staging
  labels:
    app: a2a-registry-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a2a-registry-staging
  template:
    metadata:
      labels:
        app: a2a-registry-staging
    spec:
      containers:
      - name: a2a-registry
        image: gcr.io/a2a-registry-dev/a2a-registry:latest
        ports:
        - containerPort: 8000
        env:
        - name: LOG_LEVEL
          value: "DEBUG"  # More verbose logging for staging
        - name: HOST
          value: "0.0.0.0"
        - name: PORT
          value: "8000"
        - name: STORAGE_TYPE
          value: "file"
        - name: STORAGE_DATA_DIR
          value: "/data"
        - name: ENVIRONMENT
          value: "staging"
        volumeMounts:
        - name: registry-data
          mountPath: /data
        resources:
          requests:
            memory: "64Mi"  # Lower resources for staging
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: registry-data
        persistentVolumeClaim:
          claimName: registry-data-staging-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: a2a-registry-staging-service
  labels:
    app: a2a-registry-staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: a2a-registry-staging
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data-staging-pvc
  labels:
    app: a2a-registry-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi  # Smaller storage for staging
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: a2a-registry-staging-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "a2a-registry-staging-ip"
    networking.gke.io/managed-certificates: "a2a-registry-staging-cert"
    networking.gke.io/v1beta1.FrontendConfig: "a2a-registry-staging-frontend-config"
spec:
  rules:
  - host: staging.api.a2a-registry.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: a2a-registry-staging-service
            port:
              number: 80
  - host: staging.registry.a2a-registry.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: a2a-registry-staging-service
            port:
              number: 80 