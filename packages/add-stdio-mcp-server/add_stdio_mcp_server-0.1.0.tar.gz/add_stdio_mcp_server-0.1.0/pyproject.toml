[project]
name = "add-stdio-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "成对", email = "1938116316@qq.com" }
]
requires-python = ">=3.12.10"
dependencies = [
    "mcp[cli]>=1.12.3",
]

[project.scripts]
add-stdio-mcp-server = "add_stdio_mcp_server:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
