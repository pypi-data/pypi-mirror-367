[project]
name = "h2ssscam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "astropy==6.1.7",
    "matplotlib==3.10.5",
    "numpy==2.2.6",
    "scipy==1.15.3",
]


[tool.setuptools]
package-dir = {"" = "src"}
packages = ["h2ssscam"]
include-package-data = true

[tool.setuptools.package-data]
mypackage = ["data/*.npz"]

[package.scripts]
h2ssscam = "src.h2ssscam.core:main"