[project]
name = "license-api-py"
version = "0.1.4"
description = "License API connector for Python"
readme = "README.md"
authors = [
    { name = "Nazar", email = "je3nsoff@icloud.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.11.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
