[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "himile_model_middleware"
version = "0.2.0"
authors = [{ name = "qhw", email = "20244111@himile.net" }]
description = "A VLLM middleware package"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiomysql>=0.2.0",
    "vllm>=0.8.4",
    "PyYAML>=6.0.2",
    "starlette>=0.46.2",
    "fastapi>=0.115.12",
]
