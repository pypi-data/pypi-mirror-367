[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "machc.core"
version = "0.0.1"
description = "Provides entities and interactors for the e-commerce business domain."
authors = [
    { name = "Viktor Tovstyi", email = "viktor.tovstyi@gmail.com" }
]
requires-python = "==3.8.0"
readme = "README.md"