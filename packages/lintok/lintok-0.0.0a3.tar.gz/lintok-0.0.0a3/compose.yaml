services:
  script:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
      target: python
      args:
        WORKDIR: ${WORKDIR}
    networks:
      - lintok
networks:
  lintok:
    external: true
