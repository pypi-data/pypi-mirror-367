[project]
name = "mcp-agents-mytool"
version = "0.1.4"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Srinivas917", email = "srinivas1209pendela@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.12.3",
    "numpy>=2.3.2",
]

[project.scripts]
mcp-agents-mytool = "mcp_agents_mytool:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
