from .basecamp import Basecamp

# Endpoint classes are now accessed through the Basecamp client
# Example:
# client = Basecamp(credentials)
# message_board = client.message_board(project_id, message_board_id)
# campfire = client.campfire(project_id, campfire_id)
# attachments = client.attachments()