# .readthedocs.yml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# Python settings and pip install
python:
  install:
    - requirements: docs/requirements.txt  # Project-wide pip dependencies

sphinx:
  configuration: docs/conf.py
