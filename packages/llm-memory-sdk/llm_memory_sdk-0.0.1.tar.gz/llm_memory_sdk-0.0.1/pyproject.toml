[build-system]
requires = ["setuptools>=80.0"]
build-backend = "setuptools.build_meta"


[project]
name = "llm-memory-sdk"
version = "0.0.1"
authors = [
    { name="Stream", email="Stream_2@qq.com" },
]
description = "Currently for internal use only."
readme = "ReadMe.md"
requires-python = ">=3.13"

dependencies = [
    "pydantic>=2.11.7",
    "setuptools>=80.0",
]

[dependency-groups]
dev = [
    "mypy>=1.17.0",
    "ruff>=0.12.5",
    "pytest>=7.4.3",
    "pytest-asyncio>=0.21.0",
    "openai>=1.97.1",
    "pytest-cov>=4.1.0",
]
