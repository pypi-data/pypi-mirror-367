[build-system]
requires = ["setuptools", "wheel"]      # ابزارهای لازم برای بیلد
build-backend = "setuptools.build_meta"
[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
[project]
name = "fars_tts"
version = "0.1"
description = "Use pertian tts so easy."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Amirali", email = "Amir.z.nine.three@gmail.com"}
]
dependencies = [
  # "asyncio",
  # "edge-tts"
]
license = { text = "MIT" }
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]