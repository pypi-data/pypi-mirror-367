[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ordinal_xai"
version = "0.2.0"
description = "Ordinal regression explainability library."
authors = [
    { name = "Jakob Wankmüller" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "torch",
    "gower",
    "sphinx",
    "sphinx_rtd_theme"
]

[tool.setuptools.packages.find]
where = ["ordinal_xai"] 