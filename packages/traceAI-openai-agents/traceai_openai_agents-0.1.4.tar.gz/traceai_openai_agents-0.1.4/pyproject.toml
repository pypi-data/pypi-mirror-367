[tool.poetry]
name = "traceAI-openai-agents"
version = "0.1.4"
description = "OpenTelemetry instrumentation for OpenAI Agents"
authors = ["Future AGI <no-reply@futureagi.com>"]
readme = "README.md"

packages = [
    { include = "traceai_openai_agents" },
]

[tool.poetry.dependencies]
python = ">3.9, <3.14"
openai-agents = ">= 0.0.7"
fi-instrumentation-otel = ">=0.1.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
