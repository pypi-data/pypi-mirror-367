"""
yaapp plugins - extending functionality through discovery system.

Plugins are discovered automatically through the configuration system.
No manual imports needed - plugins register themselves via @yaapp.expose() decorator.
"""

# No manual imports - plugins are discovered automatically