[project]
name = "graphql-schema-mcp"
version = "0.9.1"
description = "Tiny MCP server to handle search in large GraphQL introspection files. Features vector search to improove experience."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faiss-cpu>=1.11.0.post1",
    "fastmcp>=2.11.2",
    "sentence-transformers>=5.1.0",
]

[project.scripts]
graphql-schema-mcp = "main:main"