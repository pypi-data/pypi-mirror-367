[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Orbeez"
version = "1.1.1"

dependencies = [
 "numpy",
 "matplotlib",
 "scipy",
 "pillow",
 "astroquery",
 "astropy",
 "tqdm"
]

[tool.setuptools]
py-modules = []