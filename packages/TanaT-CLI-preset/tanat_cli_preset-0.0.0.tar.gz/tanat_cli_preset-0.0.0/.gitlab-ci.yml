publish:
  stage: deploy
  tags: &id001
  - ci.inria.fr
  - linux
  - small
  image: python:latest
  script:
    - pip install build twine
    - python -m build
    - twine upload dist/*
  rules:
    - if: $CI_COMMIT_TAG =~ /^v.*/

stages:
  - deploy
  