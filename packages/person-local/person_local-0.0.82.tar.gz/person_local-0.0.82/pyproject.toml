# This file should be in the future instead of setup.py
# https://python-poetry.org/docs/pyproject

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]

name = "person-local"
# I believe we are still using the version from setup.py and not from here until potery will work
version = "0.0.72" # https://pypi.org/project/<project-name> i.e. https://pypi.org/project/storage-local/
description = "person-local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]
