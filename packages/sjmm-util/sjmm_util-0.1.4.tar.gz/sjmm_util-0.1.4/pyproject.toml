requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sjmm_util"
version = "0.1.4"
readme = "README.md"

dependencies = [
 "numpy",
 "pandas",
 "matplotlib",
 "astropy",
 "reproject",
 "scipy",
 "scikit-image"
]

[tool.setuptools.packages.find]
where = ["sjmm_util"]

[tool.setuptools.package-data]
my_package = ["sjmdefaults.mplstyle"]