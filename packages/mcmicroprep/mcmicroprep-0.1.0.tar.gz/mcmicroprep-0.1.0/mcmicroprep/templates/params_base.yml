# Base parameters for MCMicro workflow
illumination_correction:
  method: default
segmentation:
  method: default
markers:
  # cycle: marker_name
  # e.g., 1: DAPI
channels:
  # channel-specific settings go here
