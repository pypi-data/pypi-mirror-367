[build-system]
requires = ["setuptools>=64", "swig>=4", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "Clothoids"
dynamic = ["version"]
authors = [{name = "Enrico Bertolazzi"}]
description = "G1 and G2 fitting with clothoids, spline of clothoids, circle arc and biarc."
readme = "src_py/README.md"

[tool.setuptools_scm]
