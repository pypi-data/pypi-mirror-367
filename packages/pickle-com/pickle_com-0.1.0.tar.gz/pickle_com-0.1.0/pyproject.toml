[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pickle_com"
version = "0.1.0"
description = "A drop-in pickle replacement compatible with legacy numpy attributes and inspect.getargspec."
authors = [
  { name="Yu Sun", email="yusunhit@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
  "numpy"
]
license = {text = "MIT"}
